class BulletList
{
    field Bullet data;
    field List next;

    constructor BulletList new(Bullet car, List cdr)
    {
        let data = car;
        let next = cdr;
        return this;
    }

    method void dispose(){
        do Memory.deAlloc(this);
        return;
    }
}