class Level
{
    field Array level1;

    constructor Level new()
    {
        let level1 = Array.new(2);
        let level1[0] = Player.new(0, 0, "right");
        let level1[1] = Brick.new(50, 50);
        return this;
    }

    method Array getLevel1()
    {
        return level1;
    }

    method void dispose()
    {
        do Memory.deAAlloc(this);
        return;
    }
}